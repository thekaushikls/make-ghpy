name: Test make-ghpy action

on:
  push:
    branches:
      - main
      - release
  workflow-dispatch:
  
jobs:
  build:
    runs-on: [windows-latest]

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3

    - name: Build Python Plugin
      uses: ./